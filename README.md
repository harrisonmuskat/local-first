# Local First


## Steps to get it running
`clone repository`
`bundle install`
`rake db:create`
`yarn install`
`rake db:migrate`

In two separate terminals
`rails s`
`yarn start`
visit localhost:3000 to visit page
